{% if canEdit is not defined or canEdit %}
<tr class='tr_add' data-assignee-id=''>
	<td class='td_number'>
		<input
			class='inputNumber alerteOpe'
			type='number'
			step="0.01"
			pattern="^\d*(\.\d{0,2})?$"
			value=''
			placeholder='0'
			{{ sign == '1' ? "min='0'" : "max='0'" }}
		/>
	</td>
	<td class='td_switch'>
		<span class='switch hide'><i class='fa-solid fa-repeat' title='Basculer entre montant fait et montant à venir' aria-label='Basculer entre montant fait et montant à venir'></i></span>
	</td>
	<td class='td_anticipe'>
		<input
			class='inputAnticipe alerteOpe'
			type='number'
			step='0.01'
			pattern='^\d*(\.\d{0,2})?$'
			value=''
			placeholder='0'
			{{ sign == '1' ? "min='0'" : "max='0'" }}
		/>
	</td>
	<td class='td_date p-1'>
		<select class='inputDay'>
			{% for i in 1..daysInMonth %}
				<option value='{{ i }}' {{ loop.index == day ? 'selected' }}>{{ i }}</option>
			{% endfor %}
		</select>/<span class='date-month'>{{ month < 10 ? '0' ~ month : month }}</span>/<span class='date-year'>{{ year }}</span>
	</td>
	<td class='td_comment p-1'>
		<input class='inputComment' type='text' />
	</td>
	<td class='td_actions row'>
		<div class="btn-group col-4 operation-action-menu" role="group">
			<button 
				type="button"
				class="btn btn-default dropdown-toggle"
				data-toggle="dropdown"
				aria-haspopup="true"
				aria-expanded="false"
			>
				<i class="fas fa-cog"></i>
				<span class="caret"></span>
			</button>
			<ul class="dropdown-menu options p-1">
				<li class='duplicateOperation' title="Dupliquer cette opération sur les mois restants de l'année en cours">Dupliquer</li>
				<li class='changeOperationDate' title="Modifier le mois et l'année de cette opération">Changer le mois/année</li>
				{% if canAssign %}
					<li class='assignOperation' title='Attribuer cette opération à une personne'>Attribuer</li>
				{% endif %}
				<li class='invalid noForm'>Retire mode édition</li>
				<li><hr></li>
				<li class="deleteAdd">Supprimer</li>
			</ul>
		</div>

		{# SHORT BUTTON DELETE #}
		<div class='offset-2 col-4'>
			<button type="button" class="btn btn-outline-success mt-2 mb-2 pt-0 pb-0 trButDelAdd" title='Retirer cette ligne'>
				<i class="fas fa-times"></i>
			</button>
		</div>
	</td>
</tr>
{% endif %}
