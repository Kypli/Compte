<div
	class="modal fade dashboard-add-modal"
	id="{{ id }}"
	tabindex="-1"
	role="dialog"
	aria-labelledby="{{ id }}Title"
	aria-hidden="true"
	data-open="{{ open ? '1' : '0' }}"
>
	<div class="modal-dialog modal-xl" role="document">
		<div class="modal-content dashboard-compte-modal">
			<div class="modal-header dashboard-compte-modal-header">
				<h5 class="modal-title" id="{{ id }}Title">{{ title }}</h5>
				<button type="button" class="close" data-dismiss="modal" aria-label="Fermer">
					<span aria-hidden="true">&times;</span>
				</button>
			</div>
			<div class="modal-body">
				{{ include(form_template, {
					'form': form,
					'hide_cancel': true,
					'form_options': {
						'action': path('tableau_bord'),
						'attr': {'id': form_id}
					}
				}) }}
			</div>
		</div>
	</div>
</div>
