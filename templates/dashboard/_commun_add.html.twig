
{% include('dashboard/_' ~ lien ~ 's.html.twig') %}

{# ADD #}
<div class="mt-4">
	<div class='div-bottom text-defaut-invers'>
		<a
			href="#"
			title="Ajouter un {{ lien }}"
			data-toggle="modal"
			data-target="#modal{{ lien|capitalize }}Add"
			role="button"
		>
			<i class="fas fa-plus"></i>
		</a>
	</div>
	<div class='div-bottom-2 text-defaut-invers'>
		<a href="{{ path(lien) }}" title="Voir la liste en détails">
			<i class="fas fa-clipboard-list"></i>
		</a>
	</div>
</div>
