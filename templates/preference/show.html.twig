{% extends 'base.html.twig' %}

{% block title %}UserPreference{% endblock %}

{% block body %}
    <h1>UserPreference</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_preference.id }}</td>
            </tr>
            <tr>
                <th>CompteGenreShow</th>
                <td>{{ user_preference.compteGenreShow ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_preference_index') }}">back to list</a>

    <a href="{{ path('app_preference_edit', {'id': user_preference.id}) }}">edit</a>

    {{ include('preference/_delete_form.html.twig') }}
{% endblock %}
