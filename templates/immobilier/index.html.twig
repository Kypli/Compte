{% extends 'base.html.twig' %}

{% block title %}Liste de vos Biens Immobiliers{% endblock %}

{% block body %}
<div class="container">

	{% include('_h1.html.twig') with {'text': 'Liste de vos Biens Immobiliers'} %}

	<table class="table">
		<thead>
			<tr>
				<th>Actions</th>
				<th>Nom</th>
			</tr>
		</thead>
		<tbody>
		{% for immobilier in immobiliers %}
			<tr>
				<td>
					<div class="conteneur">
						<div class="col-1">
							<a href="{{ path('immobilier_show', {'id': immobilier.id}) }}" title="Regarder ce bien immobilier">
								<i class="fas fa-eye"></i>
							</a>
						</div>
						<div class="col-1">
							<a href="{{ path('immobilier_edit', {'id': immobilier.id}) }}" title="Modifier ce bien immobilier">
								<i class="fas fa-wrench"></i>
							</a>
						</div>
					</div>
				</td>
				<td>{{ immobilier.libelle }}</td>
			</tr>
		{% else %}
			<tr>
				<td colspan="2">Aucun bien immobilier trouvé</td>
			</tr>
		{% endfor %}
		</tbody>
	</table>

	<div class="row">
		<a href="{{ path('immobilier_new') }}" class="btn btn-success col-3 mt-4 mb-4">
			<i class="fas fa-plus"></i>
			Créer un nouveau bien immobilier
		</a>
		<a href="{{ path('tableau_bord') }}" class="btn btn-warning offset-1 col-3 mt-4 mb-4">
			<i class="fas fa-backward"></i>
			Retour au tableau de bord
		</a>
	</div>
</div>
{% endblock %}
