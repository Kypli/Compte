{% extends 'base.html.twig' %}

{% block title %}Immobilier{% endblock %}

{% block body %}
    <h1>Immobilier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ immobilier.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('immobilier_index') }}">back to list</a>

    <a href="{{ path('immobilier_edit', {'id': immobilier.id}) }}">edit</a>

    {{ include('immobilier/_delete_form.html.twig') }}
{% endblock %}
