{% extends 'base.html.twig' %}

{% block title %}Mobilier{% endblock %}

{% block body %}
    <h1>Mobilier</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mobilier.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('mobilier') }}">Retour à la liste</a>

    <a href="{{ path('mobilier_edit', {'id': mobilier.id}) }}">Modifier</a>

    {{ include('mobilier/_delete_form.html.twig') }}
{% endblock %}
